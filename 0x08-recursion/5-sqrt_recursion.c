#include "main.h"

/**
* _sqrt_recursion - return natural square root
* @n: formal parameter
* Return: int
*/

int _sqrt_recursion(int n)
{

	if ((n / i) * i == n)
	{
		return (i);
	}
	return _sqrt_recursion(n);
}
